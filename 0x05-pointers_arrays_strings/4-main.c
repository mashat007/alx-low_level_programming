#include "main.h"
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "Two things are infinite, the universe and human stupidity - Albert E";
	print_rev(str);
	return (0);
}
